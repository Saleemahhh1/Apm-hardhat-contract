{
  "name": "apm-agro-hardhat-contracts",
  "version": "1.0.0",
  "description": "APM-Agro Token + Market Factory on BSC Testnet",
  "scripts": {
    "test": "npx hardhat test",
    "deploy": "npx hardhat run scripts/deploy.js --network bsctest"
  },
  "dependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "@openzeppelin/contracts": "^5.0.2",
    "dotenv": "^16.3.1",
    "hardhat": "^2.22.3"
  }
}
